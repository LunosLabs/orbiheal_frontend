/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  allowedDevOrigins: [
    'https://orbiheal-frontend.onrender.com',
    'http://192.168.134.80:3000'
  ],
};

export default nextConfig;
